// TEMPLATE FILE #############################
// will not show in your right eye.
//############################################

shader_type spatial;

void fragment() {
    if (VIEW_INDEX == 1) discard;
}
