// TEMPLATE FILE #############################
// will not show in your left eye.
//############################################

shader_type spatial;

void fragment() {
    if (VIEW_INDEX == 0) discard;
}
